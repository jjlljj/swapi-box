import React from 'react';
import propTypes from 'prop-types';
import './Header.css';
import Nav from '../Nav/Nav'

const Header = () => {
  return (
    <div>
      <Nav />
    </div>
  );
};

export default Header;
